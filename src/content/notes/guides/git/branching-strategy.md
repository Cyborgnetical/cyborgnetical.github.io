---
title: Branching Strategy
description: A nested note, two folders deep.
order: 1
---

This note lives at `guides/git/branching-strategy.md`, which is why it shows up two
levels deep in the sidebar. Nested folders map directly to nested sections — there's
no depth limit.

A simple strategy:

1. `main` is always deployable.
2. Work happens on short-lived feature branches.
3. Merge back into `main` once reviewed.
